// === - equality operator
// !== - no equal operator
// < - less than operator
// > - greater than operator
// <= - less than equal to operator 
// >= - greater than or equal to operator

let temp = 32
let isFreezing = temp <= 32
console.log(isFreezing)

// Challenge area
/*
    Create set to your age
    Calculate is child - if they're 7 or younger
    Calculate is senior - if they're 65 or older
    Print is child value
    Print is senior value
*/
let age = 34
let isChild = age <= 7
let isSenior = age >= 65
console.log(isChild)
console.log(isSenior)